import React from 'react';
import styled from 'styled-components';

function FeaturesBlock() {
    return(
        <div>
            This is the Features Block.
        </div>
    );
}

export default FeaturesBlock;